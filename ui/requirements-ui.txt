# The UI's entire dependency set. It talks to the API over HTTP and imports
# nothing from src/, so none of qdrant-client, langchain, langgraph, psycopg2,
# pymupdf or sentence-transformers appear here — which is what keeps the UI
# image small and independent of the backend's.
#
#   pip install -r ui/requirements-ui.txt
#   streamlit run ui/app.py

# 1.60 is the floor because ui/app.py pins the ask row with `st.bottom`,
# which became public API in that release (`st._bottom` before, now
# deprecated). Everything else here works far further back.
streamlit>=1.60
requests>=2.31
pandas>=2.0
